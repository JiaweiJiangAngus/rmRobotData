# 连接网络
# ./build.bash 编译并抓取最新数据
# ./run.bash 进入查询，并自动用默认浏览器打开网页
# 查询后会在 bin/robot_dashboard.html 生成网页报告，直接用浏览器打开即可

# GitHub Pages
# ./publish_pages.bash 根据当前 data/ 生成 docs/index.html
# ./publish_pages.bash --fetch 先抓取最新数据，再更新 docs/index.html
# GitHub 仓库 Settings -> Pages 选择 master 分支的 /docs 目录即可建站
